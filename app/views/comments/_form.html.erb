<%= form_for([@producto, @producto.comments.build]) do |f| %>
  <p>
    <%= f.label "Escriba su comentario:" %><br>
    <%= f.text_area :mensaje , cols:100, rows:5 %>
  </p>
  <p>
    <%= f.submit "Comentar" %>
  </p>
<% end %>