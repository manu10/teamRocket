<p id="notice"><%= notice %></p>


<img src="<%=@producto.UrlImage%>" alt="Sin Imagen" class="img-thumbnail">

<p>
  <strong>Titulo:</strong>
  <%= @producto.titulo %>
</p>

<p>
  <strong>Descripcion:</strong>
  <%= @producto.descripcion %>
</p>

<p>
  <strong>Vencimiento:</strong>
  <%= @producto.vencimiento.strftime("%d de %B de %Y") %>
</p>

LO SIGUIENTE ES SOLO PARA CONTROL
<!-- LO SIGUIENTE ES SOLO PARA CONTROL -->
<p>
  <strong>Usuario:</strong>
  <%= @producto.user_id %>
</p>

<h2>Comentarios</h2>
<%= render @producto.comments %>
</p>


<h2>Añadir un comentario:</h2> 
<%= render "comments/form" %> 
</p> 
  
<% if user_signed_in? %>
	<% if current_user.id=@producto.user_id %>
		<%= link_to 'Editar producto', edit_producto_path(@producto) %>
	<% else %>
		<%link_to 'Realizar oferta','#' %> 
	<% end %>
<%end %>
<%= link_to 'Atras', productos_path %>
