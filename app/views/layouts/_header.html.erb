 <header>
      <nav class="navbar navbar-fixed-top  navbar-inverse" role="navigation" id= "header" >
          
          <!-- El logotipo y el icono que despliega el menu se agrupan
               para mostrarlos mejor en los dispositivos moviles -->
          <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse"
                    data-target=".navbar-ex1-collapse">
              <span class="sr-only">Desplegar navegacion</span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </button>
         <%= link_to root_path, class: 'navbar-brand center-block' do %>
                    <%= image_tag "logo.png", title: "Hecho con el corazon", class: "img-thumbnail"%>
                    <!--BestNid-->
                  <% end %></a>


          </div>
         
          <!-- Agrupar los enlaces de navegacion, los formularios y cualquier
               otro elemento que se pueda ocultar al minimizar la barra -->
          <div class="collapse navbar-collapse navbar-ex1-collapse ">
            <ul class="nav navbar-nav">
              <li ><a href= "/contacts/new" >Contactar al administrador</a></li>
              
            </ul>
         
          
        <ul class="nav navbar-nav navbar-right">
        <%= form_tag(productos_path, :method => "get", id: "search-form", class:'navbar-form navbar-left') do %>
        <%= text_field_tag :search, params[:search], placeholder: "Buscar",class:'form-control' %>

        <%= button_tag(:type => 'submit', :class => 'btn btn-default') do %>
         <span class="glyphicon glyphicon-search"></span>
        <% end %>
        <% end %>
         </ul>

   
            <ul class="nav navbar-nav navbar-right">
               <% if user_signed_in? %>
          
          <div class="nav navbar-nav navbar-right"><li><%= link_to "Cerrar sesion", destroy_user_session_path, method: :delete %></li><br></div>
          <div class="nav navbar-nav navbar-right"> <li><%= link_to 'Ver Perfil', user_show_path %> </li></div>

        <% else %>
          <li><%= link_to "Registrarse", new_user_registration_path, :class => 'navbar-link'  %> </li>
          <li><%= link_to "Iniciar sesion", new_user_session_path, :class => 'navbar-link'  %></li>
        <% end %>
             
            </ul>
          </div>
          <% if user_signed_in? %>
            <div class="row center"> 
              <strong id="logged">Sesion iniciada como <%= current_user.email %>  
                <%if current_user.role_id==2%>
                   (<%= Role.find(current_user.role_id).name%>)
                <%end%>
              </strong>
            </div>
          <%end%>
    </nav>

</header>
