
<% if @productos.size==0 %>
<h1>No se encontraron subastas</h1>
        
      <% else %>
      
      <h1>Subastas Vigentes  </h1>


  <div class="row">

  <% if user_signed_in? %>
<%= link_to 'SUBASTAR!', new_producto_path, class:"btn btn-primary ",type:"button" %>
<% end %>
     <div class="btn-group">
  <button type="button" class="btn btn-info dropdown-toggle" data-toggle="dropdown">
    Ordenar <span class="caret"></span>
  </button>
  <ul class="dropdown-menu" role="menu">
  
                <li><%= link_to 'Alfabeticamente', sort: "titulo", direction:"asc" %></li>

                <li><%= link_to 'Mas recientes', sort: "created_at", direction:"asc" %></li>

                <li><%= link_to 'Mas antiguas', sort: "created_at", direction:"desc" %></li>

                <li><%= link_to 'A punto de expirar', sort: "vencimiento", direction:"asc" %></li>

                <li><%= link_to 'Mucho tiempo para que caduquen', sort: "vencimiento", direction:"desc" %></li>

  </ul>
  
</div>

</div>


<%= will_paginate %>

<div class="row">
    <% @productos.each do |producto| %> 
  <div class="row container center subasta" >
  <dt><%= producto.titulo %> </dt>
  <img src="<%=producto.UrlImage%>" class="img-thumbnail test" alt="SIN IMAGEN" style="max-width: 15%; max-height: 15%;">

  <dt><%= producto.descripcion[0, 99] %>
              <% if producto.descripcion.length > 100 %>
                <span> <%= link_to "(ver más...)", producto %></span>
              <% end %></dt>
  <dd></dd>
  <dt>Publicado: </dt>
  <dd><%= producto.created_at %></dd>
  <dt>Vencimiento: </dt>
  <dd><%= producto.vencimiento %></dd>
  <%= link_to 'Mostrar producto', producto, class:'btn btn-default' %>
  <% if user_signed_in? and current_user.id=producto.user_id %>
  <%= link_to 'Editar producto', edit_producto_path(producto), class:'btn btn-default' %>
  <%= link_to 'Eliminar producto', producto, method: :delete, data: { confirm: '¿Estas seguro?' }, class:'btn btn-danger' %>
  <% end %>
  </div>
  <% end %>
<% end %><br>
  </div>
  <%= will_paginate %>



